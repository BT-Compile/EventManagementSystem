@page
@model EventManagementSystem.Pages.Attendee.IndexModel
@{
}

<h1 class="display-6">Welcome @HttpContext.Session.GetString("RoleType"),</h1>
<h2 class="display-6">@Model.FullName</h2>
<br />
<h4>You are currently on team: @Model.TeamName</h4>
<h6>Want to sign up / change teams? Click <a href="/Attendee/TeamSignUp">here</a></h6>
<br />
<h4>Your scheduled programs:</h4>

<table class="table table-bordered table-responsive table-hover">
    <thead>
        <tr>
            <th>Event Name:</th>
            <th>Event Description:</th>
            <th>Start Date:</th>
            <th>End Date:</th>
            <th>Activity Name:</th>
            <th>Activity Description:</th>
            <th>Activity Date:</th>
            <th>Start Time:</th>
            <th>End Time:</th>
            <th>Building Name:</th>
            <th>Room Number:</th>
        </tr>
    </thead>
    <tbody>
        @{
            foreach (var record in Model.attendeeSchedules)
            {
                <tr>
                    <td>@record.EventName</td>
                    <td>@record.EventDescription</td>
                    <td>@record.StartDate</td>
                    <td>@record.EndDate</td>
                    <td>@record.ActivityName</td>
                    <td>@record.ActivityDescription</td>
                    <td>@record.StartTime</td>
                    <td>@record.EndTime</td>
                    <td>@record.BuildingName</td>
                    <td>@record.RoomNumber</td>
                    <td>
                        <a asp-page="AttendeeSignUp/Cancel" class="btn btn-outline-danger" asp-route-activityid="@record.ActivityID">Cancel</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

<br />

<a class="btn btn-outline-light" href="/Attendee/AttendeeSignUp">Sign up for an activity</a>
