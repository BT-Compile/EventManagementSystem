@page
@model EventManagementSystem.Pages.Attendee.AttendeeSignUp.IndexModel
@{
}

<h2>Available Activities</h2>
<table class="table table-bordered table-responsive table-hover">
    <thead>
        <tr>
            <th>Event Name:</th>
            <th>Event Description:</th>
            <th>Activity Name:</th>
            <th>Activity Description:</th>
            <th>Date:</th>
            <th>Time:</th>
            <th>Building:</th>
            <th>Room Number:</th>
        </tr>
    </thead>
    <tbody>
        @{
            foreach (var record in Model.attendeeSchedules)
            {
                <tr>
                    <td>@record.EventName</td>
                    <td>@record.EventDescription</td>
                    <td>@record.ActivityName</td>
                    <td>@record.ActivityDescription</td>
                    <td>@record.Date.ToShortDateString</td>
                    <td>@record.StartTime</td>
                    <td>@record.BuildingName</td>
                    <td>@record.RoomNumber</td>
                    <td>
                        <a asp-page="Confirmation" class="btn btn-outline-success" asp-route-activityid="@record.ActivityID">Sign up</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

<br />

<a asp-page="../Index" class="btn btn-secondary">Cancel</a>

<br />
<br />
<br />
<br />