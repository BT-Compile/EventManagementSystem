@page "{UserID}"
@model EventManagementSystem.Pages.Users.ViewUserModel
@{
    ViewData["Title"] = "View User";
}

<div class="wrapper">
    <br />
    <h2 class="font centertxt">Fullname</h2>
    <br />
    <table class="table table-responsive table-hover">
        <thead>
            <tr>
                <th>First Name:</th>
                <th>Last Name:</th>
                <th>Email:</th>
                <th>Phone Number:</th>
                <th>Username:</th>
                <th>Accomodation:</th>
                <th>Event Names:</th>
                <th>Dates:</th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (var data in Model.UserData)
                {
                    <tr>
                        <td>@data.FirstName</td>
                        <td>@data.LastName</td>
                        <td>@data.Email</td>
                        <td>@data.PhoneNumber</td>
                        <td>@data.Username</td>
                        <td>@data.Accomodation</td>
                        <td>@data.EventName</td>
                        <td>@data.Date</td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>