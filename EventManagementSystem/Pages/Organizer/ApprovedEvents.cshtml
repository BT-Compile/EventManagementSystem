@page
@model EventManagementSystem.Pages.Organizer.ApprovedEventsModel
@{
}

<div class="wrapper">
    <br />
    <h2 class="font">Your Approved Events:</h2>
    <br />
    <form method="post" asp-page="Organizer/ApprovedEvents">
        <input type="text" asp-for="InputString" placeholder="Search Events" required pattern=".*\S+.*" title="Input must contain at least one non-whitespace character" />
        &emsp;<button type="submit" class="btn rndbtn btn-light">Search</button>
    </form>
    <br />

    <table class="table table-responsive table-hover">
        <thead>
            <tr>
                <th>Event Name:</th>
                <th>Event Description:</th>
                <th>Start Date:</th>
                <th>End Date:</th>
                <th>Registration Deadline:</th>
                <th>Event Type:</th>
                <th>Event Status:</th>
                <th>Date Approved:</th>
                <th>Event Details:</th>
            </tr>
        </thead>
        <tbody class="table-secondary">
            @{
                foreach (var record in Model.Events)
                {
                    <tr>
                        <td><b>@record.EventName</b></td>
                        <td>@record.EventDescription</td>
                        <td>@record.StartDate</td>
                        <td>@record.EndDate</td>
                        <td>@record.RegistrationDeadline</td>
                        <td>@record.EventType</td>
                        <td>@record.Status</td>
                        <td>@record.RequestDate</td>
                        <td>
                            <a asp-page="/Organizer/ViewEvent" class=" btn rndbtn btn-light" asp-route-eventid="@record.EventID">View/Edit</a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>